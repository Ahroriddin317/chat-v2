import React from 'react'

const WorkSpaceIcon = ({ imgUrl }) => {
  return (
    <div>
      <img src={`${imgUrl}`} alt="Work space icon" />
    </div>
  )
}

export default WorkSpaceIcon
